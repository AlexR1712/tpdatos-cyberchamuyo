INSTRUCCIONES PARA EJECUTAR:
Mantener todos los directorios (config, inputFiles, outputFiles)
Mantener (por defecto) dictionary2.txt, frases-celebres.txt y stopwords.txt (el nombre que el programa usa para estos archivos puede modificarse en config)
Mantener los archivos de la carpeta config
Correr el makefile en el directorio raiz del programa (en el cual viene por defecto)
Correr el programa
Para salir utilizar el comando "salir"
En caso de salida forzada (sin utilizar el comando salir) deben borrarse todos los archivos de outputFiles y todos los binarios
